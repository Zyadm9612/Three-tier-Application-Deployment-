FROM node:26.3.0-alpine 
WORKDIR /app 
COPY package*.json ./ 
ARG REACT_APP_BACKEND_URL
ENV REACT_APP_BACKEND_URL=$REACT_APP_BACKEND_URL
RUN npm install 
RUN npm install && npm update ws webpack-dev-middleware url-parse terser sha.js 
COPY . . 
EXPOSE 3000
CMD [ "npm" , "start" ] 
